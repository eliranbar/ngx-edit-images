/**
 * Phase 3 stubs — secondary entry points for PSD / RAW / color management
 * will live under projects/ngx-image-editor/psd and /raw.
 * Feature IDs already exist in NIE_FEATURES (psd, raw, colorManagement).
 */
export const PHASE3_STUBS = {
  psd: 'Coming in a future release — @ebdev/ngx-image-editor/psd',
  raw: 'Coming in a future release — @ebdev/ngx-image-editor/raw',
  colorManagement: 'Coming in a future release',
} as const;
